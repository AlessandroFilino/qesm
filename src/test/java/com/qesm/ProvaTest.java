package com.qesm;

import org.jgrapht.graph.DirectedAcyclicGraph;
import org.junit.jupiter.api.Test;
import org.oristool.eulero.modeling.stochastictime.UniformTime;

public class ProvaTest {

    // @Test
    // public void test() {
    // DirectedAcyclicGraph<ProductType, CustomEdge> dag = new
    // DirectedAcyclicGraph<>(CustomEdge.class);

    // ProductType v0 = new ProductType("v0");
    // ProductType v1 = new ProductType("v1", 10, new UniformTime(0, 2));
    // dag.addVertex(v0);
    // dag.addVertex(v1);

    // DirectedAcyclicGraph<ProductType, CustomEdge> dag2 =
    // (DirectedAcyclicGraph<ProductType, CustomEdge>) dag
    // .clone();
    // dag2.removeVertex(v1);

    // System.out.println(dag);
    // System.out.println(dag2);
    // }

}
